FROM ubuntu:22.04 AS runtime

RUN apt-get update && apt-get install -y \
    ca-certificates \
    && apt-get clean \
    && rm -rf /var/lib/apt/lists/*

WORKDIR /app

COPY ./conf ./conf
COPY ./migrations ./migrations
COPY ./bin/opxs-api /usr/local/bin/

ENTRYPOINT ["/usr/local/bin/opxs-api"]
